module trans/semantics/strings

imports
  src-gen/ds-signatures/grace-sig
//  trans/semantics/values
  trans/semantics/expressions

signature
  constructors
    LineupV : List(V) -> V
    
rules

   LineupExp(Lineup(es: List(Exp))) --> LineupV(vs)
     where es --> vs.
