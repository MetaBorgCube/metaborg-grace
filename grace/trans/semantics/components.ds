module trans/semantics/values

imports
  trans/semantics/values
  trans/semantics/context

signature

  sort aliases
    OHeap = Map(Int, O)
    MHeap = Map(Int, M)
    Locals = Map(String, V)

  components
    C : Context
    S : Self
    Out : Outer
    OH : OHeap
    MH : MHeap
    L : Locals