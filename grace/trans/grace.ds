module trans/grace

imports
  trans/semantics/semantics
  
signature
  arrows
    Program -init-> (V * H)

rules

  p@Program(_, _) -init-> (v, H2)
  where
    create-base-object() :: H {} --> RefV(F) :: H1; 
    BaseF F, F, P Exec() |- p :: H1 --> v :: H2.
