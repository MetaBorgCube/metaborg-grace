module syntax-blocks

language grace

start symbol Program

test just empty block [[
{};
]] parse succeeds

test method call in block [[
{ do.something; };
]] parse succeeds

test block with argument [[
{ i -> i + 1; };
]] parse succeeds

test block with 2 arguments [[
{ sum, next -> sum + next; };
]] parse succeeds


